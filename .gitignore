node_modules
dist
coverage
*.tsbuildinfo
*.local
.env
vite-dev.*.log
.worktrees/

.wrangler
.dev.vars*
!.dev.vars.example
!.env.example

# Cache local do CLI do Supabase. Muda a cada comando rodado; não é fonte de
# verdade de nada e não deve gerar diff a cada `supabase db push`/`login`.
supabase/.temp/
